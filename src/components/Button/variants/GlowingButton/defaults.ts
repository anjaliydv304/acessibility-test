import { GlowingButtonProps } from './types';

export const glowingButtonDefaults: Partial<GlowingButtonProps> = {
  label: 'Shiny Click',
  disabled: false,
  type: 'button',
  intensity: 'medium',
};
